[package]
name = "nexus-provider-digitalocean"
version = "0.1.0"
edition = "2021"
description = "EP-036 DigitalOcean provider binding identity for the Compute Fabric contract (SPEC-016). M1 declares the provider binding; real API integration is owned by later milestones."
license = "MIT"

[dependencies]
nexus-compute = { path = "../../crates/nexus-compute", version = "0.1.0" }
nexus-domain = { path = "../../crates/nexus-domain", version = "0.1.0" }
serde.workspace = true
serde_json.workspace = true
