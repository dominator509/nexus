name: nexus_mobile_e2e
description: "Nexus EP-034 M3 integration tests: real HTTP transport (dart:io) carrying canonical mobile contract JSON across a real socket boundary."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  nexus_mobile:
    path: ../../../apps/mobile
  nexus_mobile_contracts:
    path: ../../../packages/mobile-contracts

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
