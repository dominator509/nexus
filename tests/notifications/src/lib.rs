//! EP-032 M4 notification failure/abuse e2e proofs (SPEC-014
//! behavior 7). The actual tests live in `tests/e2e.rs`; this lib
//! target exists only so the crate is a workspace member with a
//! stable package name (the failure suite is test-only).

#![forbid(unsafe_code)]
