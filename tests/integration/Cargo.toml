[package]
name = "nexus-test-execution"
version = "0.1.0"
edition = "2021"
description = "EP-040 M2 deterministic test execution core: real subprocess test runs, cargo-output parsing, GateResult aggregation, flake policy, resource hygiene (SPEC-008; TESTING.md)"
license = "MIT"

[dependencies]
nexus-test-contract = { path = "../contract", version = "0.1.0" }
nexus-domain = { path = "../../crates/nexus-domain", version = "0.1.0" }
serde.workspace = true
serde_json.workspace = true
