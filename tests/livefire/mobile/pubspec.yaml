name: nexus_mobile_livefire
description: "Nexus EP-034 M5 live-fire proofs: LF-004 multi-user-identity and LF-022 mobile-step-up journeys composed from REAL production components (nexus_mobile contracts + nexus_mobile_contracts behavior), with current-run machine-readable evidence."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  nexus_mobile:
    path: ../../../apps/mobile
  nexus_mobile_contracts:
    path: ../../../packages/mobile-contracts

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
