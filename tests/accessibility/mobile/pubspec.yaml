name: nexus_mobile_failure
description: "Nexus EP-034 M4 forced-failure suite: fail-closed behavior of the mobile contract/behavior layer under malformed input, denied authority, duplicate/cancelled/unavailable work, and observability redaction. Real production components; real failure mechanisms."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  nexus_mobile:
    path: ../../../apps/mobile
  nexus_mobile_contracts:
    path: ../../../packages/mobile-contracts

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
