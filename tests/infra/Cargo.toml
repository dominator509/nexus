[package]
name = "nexus-infra-failure-tests"
version = "0.1.0"
edition = "2021"
description = "EP-036 M4 forced-failure suite: real failure mechanisms (terminated container, timeout, corrupted input, denied policy, duplicate request) proving fail-closed behavior, structured errors, and reconciliation (SPEC-016)"
license = "MIT"

[dependencies]
nexus-compute = { path = "../../crates/nexus-compute", version = "0.1.0" }
nexus-domain = { path = "../../crates/nexus-domain", version = "0.1.0" }
serde.workspace = true
serde_json.workspace = true
