[package]
name = "nexus-hardware-certification"
version = "0.1.0"
edition = "2021"
description = "EP-040 M4 hardware certification behavior: simulator-vs-real distinction, device identity ladder, fake-device rejection, missing-hardware capability blocking, stale evidence rejection (SPEC-008)"
license = "MIT"

[dependencies]
nexus-test-contract = { path = "../contract", version = "0.1.0" }
serde.workspace = true
serde_json.workspace = true
