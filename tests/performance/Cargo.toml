[package]
name = "nexus-test-performance"
version = "0.1.0"
edition = "2021"
description = "EP-040 performance budget evaluation root: deterministic PerformanceBudget evaluation behind the nexus-test-contract port (SPEC-008; TESTING.md)"
license = "MIT"

[dependencies]
nexus-test-contract = { path = "../contract", version = "0.1.0" }
nexus-domain = { path = "../../crates/nexus-domain", version = "0.1.0" }
serde.workspace = true
serde_json.workspace = true
