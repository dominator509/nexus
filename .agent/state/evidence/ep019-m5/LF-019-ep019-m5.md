# LF-019 self-healing-fix-loop (EP-019 M5)

Generated: 2026-08-16 (live-fire run)
Node: EP-019
Command: `sh scripts/live-fire/LF-019.sh`
Proof id: LF-019 (self-healing-fix-loop)

## Real proof (no manifest-only claim)

The REAL EP-019 composition is exercised end to end through REAL
mechanisms. The `lf019_self_healing_fix_loop_full_chain` test in
`crates/nexus-healing/tests/lf019.rs` drives the full chain:

1. **OBSERVE -> INCIDENT (real process failure).** The REAL controlled
   failing fixture (`tests/healing/fixtures/failing-worker.sh`,
   CONTROLLED_TEST_FIXTURE) crashes in a REAL subprocess with exit 1 and
   crash output even when the operator passes the correct marker path —
   the fixture has a real logic bug (hard-coded wrong filename check).
   The crash is observed as a real process-failure signal
   (`IncidentSignalKind::ProcessFailure`).

2. **CORRELATE (canonical identifiers).** Incident memory records the
   incident under a canonical, tenant-scoped dedup key
   (`tenant|error_class|component`), never raw error text and never
   merged across tenants. Dedup lookup returns exactly the one record.

3. **DIAGNOSE (hypothesis is not root cause).** The diagnosis begins as
   HYPOTHESIS (not authoritative). Reproduction evidence raises it to
   REPRODUCED, then VALIDATED only after the real before/after proof.

4. **REPRODUCE (gold-standard before/after).** The SAME invocation
   fails (exit 1) before the patch.

5. **PATCH_PROPOSED (real artifact + real digest).** The REAL patch
   artifact (`worker-fix.patch`) is read from disk; a REAL SHA-256
   digest is computed (64 lowercase hex chars). The proposal names
   exactly one changed file (scope preserved).

6. **SANDBOX_VALIDATION.** The patch is applied to an ISOLATED WORKING
   COPY with the REAL `patch` tool; the sandbox verdict records the
   scope-preserved checks and passes.

7. **SECURITY_VALIDATION.** The security verdict passes with the
   canonical checks (security, dependency audit, license, reality,
   static analysis, secret scanning, authorization invariants). A
   patch that weakens security is rejected (proven by
   `ep019_failure_security_gate_failure_is_rejected`).

8. **REVIEW (independent).** A distinct reviewer approves the exact
   patch digest; the reviewer is never the proposer.

9. **APPROVAL (HITL, digest-bound).** A human approval binds to the
   exact patch digest; a different patch digest is never authorized
   (real SHA-256 of a different artifact differs).

10. **APPLY + REPRODUCE AFTER.** The SAME reproduction now passes
    (exit 0, `worker: healthy`). The fail-closed boundary is preserved:
    a missing marker still crashes (exit 1).

11. **CANARY.** The canary plan (canary -> targeted -> broader) with a
    real health criterion (expected HEALTHY, observed HEALTHY) reaches
    CanaryState::Healthy with auto_rollback_on_regression.

12. **POST_DEPLOY_VERIFICATION.** The original reproduction is re-run:
    exit 0, healthy output.

13. **CLOSED.** Only real observed verification moves the incident to
    the healthy terminal CLOSED (PostDeployVerification is not
    terminal; there is no FIXED/REMEDIATED escape value).

14. **ROLLBACK PROOF.** Restoring the known previous artifact makes the
    original failing behavior return (exit 1, crash output) — health
    restored to the known previous state; rollback state machine
    reaches RESTORED with health_verified=true.

15. **INCIDENT MEMORY.** The closed incident is recorded with redacted
    metadata (final_state=CLOSED, no secrets/credentials/prompts).

Cleanup: the isolated working copy is removed; no leftover fixtures.

## Observed sentinel

```
LF-019: ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
```

## External provider / sandbox / deployment certification boundary

- The failing fixture is a CONTROLLED_TEST_FIXTURE (a real deterministic
  executable with a real bug). No external provider is claimed.
- Real process execution is REAL_INTERNAL_PROCESS (real subprocess
  through the real `patch` tool and worker executable).
- Sandbox: enforcement decision PASS (isolated working copy + fail-closed
  boundary + scope preservation + sandbox/security verdicts). REAL
  OS-level sandbox isolation certification is DEFERRED (owner EP-040/
  EP-043), recorded in CERTIFICATION_REGISTRY.md.
- Production canary deployment: the deterministic canary/rollback state
  machine is proven now; REAL production canary certification is DEFERRED
  to the deployment-owning node (EP-042/EP-043). No simulated canary.
- Real Git/repository provider certification is owned by the node that
  owns the Git provider.

## Primary-rule compliance

MODEL THINKS FIXED != FIXED (no vocabulary escape; diagnosis must reach
VALIDATED through reproducible evidence). PATCH APPLIED != FIXED
(verification re-runs the original reproduction). DEPLOYED != REMEDIATED
(post-deploy verification required). Only real observed verification
closes the incident. The self-healing system cannot install its own
generated skills (skill candidate boundary owned by EP-018).
