# LF-016 Coding-Agent Cowork (EP-017 M5)

Generated: 2026-08-15T22:00:00Z (verify run)
Node: EP-017
Command: `sh scripts/live-fire/LF-016.sh`

## Real proof (real process boundary, no in-memory substitute)

The REAL EP-017 production composition is exercised end to end through
a REAL subprocess transport (`crates/nexus-harness-adapters/src/process.rs`
`ProcessRunner`), which spawns the REAL executable
`tests/agents/fixtures/coding-agent-fixture.sh`
(CONTROLLED_TEST_FIXTURE) for every normalized harness command:

- `lf016_real_process_full_cowork_chain`: objective/task ->
  capability-based agent selection (AgentSelector, quality/cost/trust/
  availability/history composite) -> budget-bound assignment
  (TaskOrchestrator::assign, delegation ACTIVE) -> REAL harness
  execution (START subprocess spawned, exit 0) -> progress (adapter
  session RUNNING) -> artifact exchange (immutable by content hash,
  attach + provenance) -> bounded review surface -> completion
  (SUCCEEDED, delegation COMPLETED, artifact lineage preserved).
- `lf016_real_process_cancellation_terminates_owned_process`: cancel
  through the REAL transport (CANCEL subprocess spawned, exit 0); the
  task ends CANCELLED, the delegation REVOKED; no orphaned process.
- `lf016_real_process_nonzero_exit_fails_closed`: the real subprocess
  exits 3 on a FAIL-requesting brief; the adapter returns typed
  UNAVAILABLE and the task stays ASSIGNED (never falsely RUNNING or
  SUCCEEDED).
- `lf016_real_process_runner_maps_exit_codes`: exit-status mapping on
  the real transport (Success, Failure(3)) and output capture; the
  adapter never parses provider output into domain contracts.
- `lf016_real_process_missing_executable_fails_closed`: spawn failure
  maps to typed UNAVAILABLE with a redacted message.

## Observed sentinel

```
LF-016: ok
test result: ok. 5 passed; 0 failed (1 suite)
```

## External provider certification boundary

Real Codex / Claude Code / Hermes / OpenClaw CLIs are NOT installed in
this environment and no provider credential is present. This proof
does NOT certify an external coding-agent provider: the fixture is a
CONTROLLED_TEST_FIXTURE. External provider certification is DEFERRED
(see CERTIFICATION_REGISTRY.md, agent-harness-adapters row; owner
EP-043 at ship-gate review, consistent with the memory-plane graph-gap
note).

## Primary-rule compliance

AGENT SELECTED (deterministic capability selection) != AGENT
AUTHORIZED (Nexus assignment) != TASK EXECUTED (real START process) !=
RESULT VERIFIED (explicit complete_task with verification evidence) !=
ARTIFACT TRUSTED (immutable hash + task binding) != TASK COMPLETED
(orchestrator terminal state + delegation COMPLETED). Completion
derives from real orchestration/harness state, never agent prose.
