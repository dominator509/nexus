# LF-021 Model Provider Failover (EP-015 M5)

Generated: 2026-08-15T05:06:17Z
Node: EP-015
Command: sh scripts/live-fire/LF-021.sh

## Real proof (no mocks, no miniature router)
- crates/nexus-model-router/tests/lf021.rs (8 tests) exercises the
  production failover surface through the REAL EP-014
  DeepSeekFlashProvider and DeepSeekReflexTransport (pinned ureq)
  against real controlled HTTP endpoints.
- DeterministicModelRouter::route_with_failover + ProviderFailoverPolicy
  (config-driven from config/models/router/policy.json failover section).

## Primary baseline
PASS

## Primary attempted before failover
PASS (real transport attempt; connection-refused -> UNAVAILABLE,
silent peer -> TIMEOUT/UNAVAILABLE)

## Primary failure type
UNAVAILABLE (connection refused); TIMEOUT/UNAVAILABLE (real 30s read timeout)

## Failover policy
PASS (only UNAVAILABLE/TIMEOUT are failover-eligible; typed lock)

## Configured secondary selected
PASS (production DeepSeekFlashProvider adapter instance at a real
isolated HTTP secondary endpoint; instance label
deepseek-v4-flash-secondary)

## Secondary real transport
PASS (real HTTP transport to the secondary endpoint)

## Final NexusControlObject validation
PASS (canonical NexusControlObjectValidator; schema_version 1.0.0)

## Schema preserved
PASS (final object retains schema_version 1.0.0; malformed secondary
response fails closed)

## Trace ID preserved
PASS (one logical correlation id c-lf021 across decision, primary
attempt, failure audit, failover decision, secondary attempt, final
result, and every audit record)

## Budget preserved / not reset
PASS (primary attempt consumes 100 milli-cost + 100 ms per policy;
secondary receives the remaining 900, never a fresh cap; final route
within max_provider_attempts=2)

## Bounded attempt count
PASS (2 attempts max; no provider cycling)

## Security override
PASS (SECRET privacy prohibits a CHEAP_API secondary; fail closed
without using the prohibited provider)

## Double-provider failure fail-closed
PASS (primary + secondary unavailable/invalid -> typed failure;
no fabricated control object)

## Audit chain
PASS (ordered RouteAuditRecord stages: PRIMARY_SELECTED,
PRIMARY_ATTEMPTED, PRIMARY_FAILED:<typed>, FAILOVER_ELIGIBLE,
SECONDARY_SELECTED, SECONDARY_ATTEMPTED, SECONDARY_VALIDATED,
ROUTE_COMPLETED; FAILED_CLOSED on fail-closed paths)

## Credential/prompt redaction
PASS (audit serialization contains no credential, no prompt body,
no feature domain)

## External DeepSeek provider
NOT ASSERTED (no DeepSeek credential in environment; transport
exercised against real controlled endpoints)

## External secondary vendor
NOT ASSERTED (registry preferred secondary is the bifrost gateway,
not implemented; production adapter used at a controlled endpoint)

## Microbrain status
NOT PROMOTED (DisabledMicrobrain is the V1 default; never selected)

## Model output authority
NONE (a valid final control object grants no authorization, no
capability grant, no approval, and cannot override OPA/OpenFGA)

## Observed sentinel
LF-021: ok
