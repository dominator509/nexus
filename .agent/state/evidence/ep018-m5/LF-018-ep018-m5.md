# LF-018 skill-install-and-run (EP-018 M5)

Generated: 2026-08-16 (live-fire run)
Node: EP-018
Command: `sh scripts/live-fire/LF-018.sh`
Proof id: LF-018 (skill-install-and-run)

## Real proof (no manifest-only claim)

The REAL EP-018 production composition is exercised end to end through
REAL mechanisms. 7 lf018 tests in
`crates/nexus-skills/tests/lf018.rs` drive the full chain:

- `lf018_real_chain_install_execute_revoke`: REAL skill bundle written
  on disk (payload = `tests/skills/fixtures/livefire-transform.sh`,
  CONTROLLED_TEST_FIXTURE) -> REAL SkillBundleLoader (filesystem I/O)
  -> REAL SHA-256 content hash -> manifest validation -> REAL ring
  Ed25519 signature verification (fresh keypair signs the canonical
  identity digest `name@version:content_hash`) -> proposal lifecycle
  PROPOSED -> EVAL_PENDING -> EVAL_PASSED -> AWAITING_PROMOTION ->
  human approval (distinct from proposer) -> PROMOTED -> installation
  through the REAL durable JSON-file registry -> discovery -> resolve
  for execution -> REAL subprocess execution boundary (SkillExecutor
  spawns the payload, scrubbed environment, granted READ only) ->
  observable result `transformed:hello world` (exit 0) -> revoke ->
  resolve_for_execution DENIED (POLICY) -> store reload preserves
  revocation.
- `lf018_tampered_bundle_fails_signature_and_never_executes`: one
  payload byte flipped on disk after signing -> content hash changes ->
  cryptographic verification FAILS (VERIFICATION) -> install rejected
  (CONFLICT, immutable by version) -> executor refuses the tampered
  package -> the installed original still resolves.
- `lf018_wrong_signer_fails_verification`: a signature made by a
  DIFFERENT key does not verify (VERIFICATION).
- `lf018_runtime_permission_not_granted_is_denied`: WRITE directive at
  runtime with only READ granted -> the payload exits 3 with
  `write-denied:not-granted` (denied authority is actually denied);
  a WRITE-declaring package at the SANDBOXED ceiling is rejected at
  install (POLICY).
- `lf018_agent_cannot_self_approve_installation`: a model/agent may
  PROPOSE a skill; it may not self-approve (POLICY); a distinct human
  CAN promote it.
- `lf018_evaluation_frozen_corpus_gate`: factory output must pass
  frozen evals; an empty eval corpus fails closed (VERIFICATION).
- `lf018_dependency_composition_boundary`: dependency present +
  valid -> composition succeeds and effective authority is the
  intersection (never wider than the caller grant); a missing
  dependency fails closed (NOT_FOUND).

## Observed sentinel

```
LF-018: ok
test result: ok. 7 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
```

## External provider / sandbox / network certification boundary

- The payload is a CONTROLLED_TEST_FIXTURE (a real deterministic
  executable: input -> bounded transformation -> output artifact). No
  external skill provider or public registry is claimed.
- Skill process execution is REAL_INTERNAL_PROCESS (real subprocess
  through the production SkillExecutor boundary).
- Sandbox: requirement/enforcement decision PASS (permission ceiling +
  caller-grant intersection + fail-closed execution boundary deny
  undeclared capabilities at runtime). REAL OS-level sandbox
  isolation certification is DEFERRED (recorded in
  CERTIFICATION_REGISTRY.md; owner EP-043/EP-040 at ship gate).
- Network: manifest `network_rules` are REQUESTS, never firewall
  configuration (ADR-025). Real network enforcement is owned by the
  Sentinel-class nodes, not EP-018.

## Primary-rule compliance

SKILL DISCOVERED (loader scan) != SKILL VERIFIED (real cryptographic
verification) != SKILL TRUSTED (caller trust tier) != SKILL AUTHORIZED
(proposal + distinct human approval) != SKILL EXECUTED (real subprocess
boundary) != RESULT VERIFIED (observable stdout + exit status).
MODEL/AGENT PROPOSES != MODEL/AGENT APPROVES (self-approval denied).
