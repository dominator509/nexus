# EP-007 M5 — LF-003 Live-Fire Evidence (Keycloak 26.7.0 + real WebAuthn ceremony)

Date: 2026-08-13
Node: EP-007 (authentication and passkeys)
Milestone: M5 (live-fire, operations, and node closure)
Probe: `nexus-ep007-probe` on 127.0.0.1:34424, Keycloak 26.7.0
image `quay.io/keycloak/keycloak:26.7.0@sha256:0f198be292568439d700cdbfb893e69a6009bb43a94a06a945b1d3d506c76b13`

## 1. Nexus Rust lifecycle — PASS

- `cargo test --locked -p nexus-keycloak`: 52 passed (audience representation matrix E1-E12, scalar/multi audience acceptance, account-only rejection, empty-array/numeric rejection, algorithm confusion, wrong kid, tampered signature, wrong issuer, expired token).
- `cargo test --locked -p nexus-auth`: 56 + 15 passed, including `tests/lf003.rs` (2/2: owner passkey onboarding lifecycle + step-up action digest / fail-closed).

## 2. Real Keycloak 26.7.0 — PASS

- Real OIDC discovery: `http://localhost:34424/realms/nexus/.well-known/openid-configuration` 200.
- Real Admin API provisioning (owner, client scopes `nexus` + `nexus.read`, audience/tenant mappers, flow, realm policy).
- No mock of any HTTP exchange.

## 3. Real Chrome WebAuthn registration — PASS

- `ep007_combined_ceremony.mjs`: REAL `navigator.credentials.create()` against Chrome's CDP virtual authenticator (ctap2, internal, resident, UV).
- Observed: `credentialAdded: 1`, resident `true`, rpId `localhost`, signCount `1`, credential hash `12e1ad751104a185` (run of record).
- Zero page errors, zero console errors.

## 4. Keycloak credential persistence — PASS

- Admin API `/users/{id}/credentials` shows the `webauthn-passwordless` credential after registration (real server-side persistence).

## 5. Fresh passkey authentication — PASS

- Separate authorize request in the SAME browser context; passkey-only login (no password) through the conditional-UI authenticator; REAL assertion against the SAME virtual authenticator.

## 6. Real credentialAsserted — PASS

- Observed: `credentialAsserted: 1`, same credential hash `12e1ad751104a185`, signCount `1 -> 2` (counter increment proves the assertion used the registered credential).

## 7. Authorization Code + PKCE — PASS

- Fresh PKCE pair (S256) per phase; REAL authorize -> REAL callback code (98 chars) -> REAL token endpoint exchange; both register and authenticate codes exchanged.
- Register-code single use proven earlier (reuse -> `invalid_grant`).

## 8. Production JWT signature validation — PASS

- `verify_access_token` example binary (jsonwebtoken 11.0.0 + aws-lc-rs, RS256 only, kid/JWKS selection) against the REAL JWKS of the live issuer.
- Real token: `{"accepted": true, "subject": "ce26b36c-...", "issuer": "http://localhost:34424/realms/nexus", "audiences": ["account", "nexus-app"], "scopes": ["openid", "nexus.read", "email", "profile"], "strength": "MULTI_FACTOR"}`.
- Tampered signature -> `InvalidSignature` (rejected).

## 9. Strict issuer validation — PASS

- Wrong issuer (`http://evil.example/realms/nexus`) with the real signature -> `InvalidIssuer` (rejected).

## 10. Strict audience validation — PASS

- Real token carries `aud: ["nexus-app", "account"]` (audience mapper + Keycloak built-in).
- Expected audience `nexus-app` -> accepted (membership).
- Expected audience `nexus-api` (not a member) -> `InvalidAudience` (rejected).
- Unit matrix proves `aud="account"` alone never satisfies a `nexus-app` boundary; malformed audiences (empty array, null, number, object, non-string members) fail closed.

## 11. STEP_UP/action digest lifecycle — PASS

- `tests/lf003.rs::ep007_live_fire_step_up_action_digest_and_fail_closed` green; step-up action digests bind and fail closed.

## 12. Session revocation — PASS

- Real Admin API logout: `POST /admin/realms/nexus/users/{id}/logout` -> 204; user sessions 8 -> 0.
- Refresh token after revocation -> HTTP 400 `invalid_grant` "Session not active".
- UserInfo with revoked-session access token -> HTTP 401.

## 13. Audit evidence — PASS

- `.agent/state/evidence/ep007-m5/ep007-m5-live-fire.json` (0600) records ceremony metadata (credential hash, signCount, rpId), validation results, certification status. No tokens, passwords, cookies, or private key material persisted; `no_secrets` flags asserted in the orchestrator.

## 14. Resource cleanup — PASS

- Probe container, callback server, browser profiles, `/tmp` token/state files, and diagnostic artifacts removed after evidence capture; orphan audit proves zero EP-007 containers/networks/browsers/volumes/helper processes (see teardown evidence).

## Certification language

- WebAuthn protocol/provider certification: **PASS** (real Keycloak 26.7.0 + real Chrome + standards-compliant CDP virtual authenticator; nothing mocked).
- Physical passkey hardware certification: **NOT ASSERTED** (virtual authenticator only; no physical hardware was exercised).
