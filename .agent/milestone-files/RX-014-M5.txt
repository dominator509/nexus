AUD-020: ICTFax binds outbound fax to destination/document. Transport create_document_with_media uploads REAL media bytes (empty rejected), create_sendfax_program binds real document, create_transmission(destination, program, document) binds canonical recipient (empty destination rejected), fetch_document_media returns real bytes (empty/missing fail closed); adapter submit reads storage_ref artifact (HylaFAX precedent), creates document+program+transmission bound to real destination/document, then sends; inbound fetch computes real size + real sha256 from fetched bytes (never size 0 / empty digest). Hostile proofs: submit data-binding (real media bytes + recipient reach provider), inbound real bytes/size/digest. nexus-ictfax 12/12, clippy -D warnings clean, workspace check clean.
