AUD-024: X reply binds the mention thread via the official reply object. Adapter reply() strips the x: prefix from conversation.thread_ref and calls the new transport reply_to_tweet(text, in_reply_to_tweet_id); HTTP impl posts the official X API v2 CreatePostsReply body {text, reply:{in_reply_to_tweet_id}} (id 1..=19 digits) and fails closed on missing/invalid thread ref (Validation, never a standalone post). Hostile regressions: reply wire body carries the official reply object (body-capturing fixture), create_tweet never carries a reply object, adapter end-to-end strips x: and binds mention id on the real wire. nexus-social-direct-connector 17/17 (was 15), clippy -D warnings clean, workspace check clean.
