AUD-081: deploy.sh is now a REAL deploy command, not a dry-run-only stub. --deploy verifies the release manifest digest and every artifact byte, bridges to the canonical installer contract, then executes the REAL transactional installer (backup-before-update, staged replacement, digest validation, atomic switch, verification) into the caller-provided install root; the host nexus tree is never touched. --dry-run performs real manifest/artifact verification. The phantom nexus-setup-cli reference is gone. Proofs: real end-to-end deploy installs bytes matching real artifacts; tampered artifact denied before mutation; release handoff carries sh scripts/deploy.sh --deploy.
