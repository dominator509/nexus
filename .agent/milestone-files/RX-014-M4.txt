AUD-019: router performs destination-aware Push-to-SMS fallback itself. ChannelProvider gains requires_destination() + deliver_to() (default fail-closed); new DestinationResolver port + UnboundDestinationResolver; EscalatingNotificationRouter resolves destinations and delivers SMS inside route() (unresolved -> fail-closed, zero mutation); SmsChannelProvider requires_destination=true; live-fire driver-side deliver_to workaround removed (router returns the SMS fallback receipt; driver keeps refresh-only handle). Hostile proofs: router-delivered SMS with resolver exactly once, fail-closed without resolver (no fabricated recipient, no mutation). nexus-notifications 37/37, sms 22/22, e2e 11/11, workspace check clean, clippy -D warnings clean; stale e2e/livefire fixture expiry (2026-08-21) moved to 2099-01-01 (M3 expiry gate correctly denied).
