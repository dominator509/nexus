AUD-049: tenant boundary truth. LocalArtifactStore ignored its tenant in every ArtifactStore method - index paths were index/{artifact_id}.json and backup manifests backups/{backup_id}.json on a SHARED root, so any tenant knowing an artifact id could get/verify/delete/list/set_retention another tenant's artifacts; index_path, backup_manifest_path, and metadata sidecars are now tenant-scoped (index/{tenant}/{id}.json, backups/{tenant}/{backup_id}.json, objects/{tenant}/{hash}.meta - mirroring S3's per-tenant bucket precedent), list() enumerates only the caller tenant's namespace, and every mutating/read path validates metadata.tenant/backup.tenant/plan.tenant/migration.tenant against the caller tenant and fails closed Policy on mismatch; hostile regressions over a REAL shared root: tenant B get/verify/delete tenant A artifact all NotFound with A's data intact, tenant B list sees none of A's entries, put with foreign-tenant metadata denied Policy + nothing written, set_retention cross-tenant NotFound + unchanged, create_backup with foreign tenant Policy, restore of A's backup by B NotFound; NAS inherits via pass-through; local 21/21 (6 new hostile), NAS 5/5, S3 13/13, ep037 M2+M3 gates green, clippy -D warnings clean, fmt clean.
