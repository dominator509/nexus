AUD-038: actual React PWA truth. apps/web was a framework-neutral contract package with NO browser entry - the accessibility proof scanned a server-rendered FIXTURE document, so EP-033 shipped without the actual React PWA its fallback contract required ('Ship responsive PWA first'). The web package now contains the REAL PWA entry - index.html with #root mount + manifest.webmanifest (standalone installable), src/main.tsx mounting the REAL @nexus/ui production components (DashboardShellView, CapabilityButton, ApprovalCardView, StatusBadge) over REAL @nexus/web contract state via ReactDOM.createRoot, and a Vite production build (vite.config.ts, @vitejs/plugin-react) emitting dist/index.html + JS bundle (52 modules). Hostile regressions prove the entry exists, imports production components (no mocks), builds, and server-renders the same production markup. Tauri desktop shell stays honestly NOT ASSERTED (thin signed wrapper remains future work per the fallback contract). Web 148/148 (5 new aud038), typecheck clean, EP-033 M5 gate green.
