AUD-035: osquery durable endpoint identity truth. The connector previously DISCARDED the documented enroll host_identifier (let _ = host_identifier) and minted a wall-clock node_key, so findings carried NO durable endpoint identity - evidence_ref was osquery:{query_id}:{port} and no correlation named the host. Enrollment now REQUIRES a non-empty host_identifier (Authorization fail closed, documented node_invalid shape over real sockets), a DIFFERENT host can never adopt the bound node's identity (identity-conflict denial, same host re-enroll keeps identity while the session node_key rotates), every ObservedOsqueryResult is stamped with the durable identity, and the adapter fails closed ExternalProvider on unattributable telemetry (host empty). Normalized findings bind the endpoint in BOTH evidence_ref (osquery:{host}:{query_id}:{port}:{protocol}) and correlation (host={host}:address=...:port=...) mirroring the wazuh agent= pattern. Hostile regressions: empty/whitespace host enrollment denied (unit + real socket), identity-conflict denied (unit + real socket), unattributable telemetry fails closed, observed results carry host_identifier. LF-009 proves host=srv-lab-1 flows from the REAL socket enrollment into the normalized finding and evidence endpoint_identity.
