AUD-086: no canonical command references a phantom executable. All phantom nexus-cli / nexus-setup-cli / target/release/nexusctl references are removed: rollback-drill.sh and restore-drill.sh delegate to the REAL ep043-rollback-drill.sh; migrate.sh applies REAL migrations/*.sql via psql with DATABASE_URL or fails closed; release-build.sh produces the REAL release manifest via the release-evidence CLI; backup.sh documents the REAL installer backup-before-update surface; seed-test-tenant.sh / proof-runner.sh / provider-certify.sh / hardware-certify.sh fail closed with explicit messages (no such real executable exists); LF-013 calls the real EP-027 M5 gate. Pre-existing format/lint drift that blocked the real path (Dart test file, Python line length) corrected.
