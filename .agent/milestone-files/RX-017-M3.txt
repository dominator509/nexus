AUD-044: owner ladder durable truth. OwnerBootstrapStore.recordState previously wrote ANY supplied state verbatim - OWNER_AUTHORIZED could be persisted without traversing the passkey/recovery ladder (the domain advance helper enforced the sequence but the persistence layer bypassed it); recordState now reads the owner's CURRENT durable rung and validates the transition via the contract's isValidOwnerBootstrapTransition BEFORE any UPDATE - a leap to OWNER_AUTHORIZED from a lower rung fails closed Policy, a backwards move fails closed Policy, a missing owner fails closed NotFound, and only the exact next rung (or idempotent re-assertion) is written; hostile regressions over REAL PostgreSQL: OWNER_AUTHORIZED leap denied + state unchanged, backwards move denied + state unchanged, exact-next-rung accepted, idempotent re-assert accepted, missing owner NotFound; onboarding 42/42 (4 new hostile), failure suite 24/24, nexus-setup 83/83, ep035 M1-M5 gates green, orphan guard clean.
