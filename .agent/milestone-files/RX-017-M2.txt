AUD-043: enrollment claim secret-possession truth. The durable EnrollmentTokenStore.claim previously consumed a one-time credential by ID+time ALONE - a caller knowing only the credential ID could consume it (secret verification was a separate verifySecret call); claim now requires the bootstrap secret and compares secret_hash IN THE SAME atomic UPDATE WHERE clause - a caller knowing only the ID can never consume, wrong/empty secret denies without consuming, the correct secret still works after hostile attempts; hostile regression 'denies consumption by credential ID alone' over REAL PostgreSQL proves state stays ISSUED after wrong/empty-secret attempts; concurrency race still exactly-one-wins (row lock); onboarding integration 38/38 incl new hostile, unit 7/7, failure suite 24/24, ep035 M1-M5 gates green, orphan guard clean.
