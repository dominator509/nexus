AUD-051: encryption-before-egress truth. NAS/S3/SeaweedFS adapters checked metadata.encryption.is_some() - a METADATA CLAIM - and never verified the bytes were ciphertext; the S3 test codified the defect by supplying plaintext + AES-256-GCM metadata and expecting the identical plaintext back (the live-fire harness had real AES-GCM; production adapters never used it); EncryptionMetadata now REQUIRES plaintext_hash (SHA-256 of the pre-encryption bytes, recorded by the encrypting caller) and ArtifactMetadata.verify_encryption_before_egress() checks: sensitive class must carry encryption metadata, plaintext_hash must be present, and sha256(stored bytes) != plaintext_hash - if the bytes hash to the recorded plaintext the 'ciphertext' IS the plaintext and fails closed Policy BEFORE any byte crosses the network (zero provider mutation); all three egress adapters (NAS/S3/SeaweedFS) wired to the verifier; hostile regressions over REAL backends: NAS rejects plaintext-labeled-as-encrypted Policy + no object written, S3 same + zero provider mutation (get NotFound), contract rejects empty plaintext_hash Validation; roundtrip tests now use REAL AES-256-GCM (ring dev-dep, same line as live-fire harness) proving stored bytes are ciphertext (assert_ne plaintext) and decrypt back to plaintext; tenant fixture drift in ep037_m4_failures (metadata_for hardcoded tenant(1) while tests used tenant(2..21)) exposed by M1 boundary check and fixed by threading tenant through; lf002 backup manifest path updated for tenant-scoped layout; local 23/23, NAS 6/6 (2 new hostile), S3 13/13, SeaweedFS 21/21, contract 27/27 (3 new), lf002+lf020 green, ep037 M1-M5 gates green, clippy -D warnings clean, fmt clean.
