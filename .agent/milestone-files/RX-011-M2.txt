AUD-067: the atomic update switch now preserves the current install (rename-aside, not delete) until the replacement is committed and verified; a rename/mount/filesystem failure restores the previous install at its original path instead of leaving the live installation deleted. Hostile proof: cross-device staging (/dev/shm tmpfs vs /tmp) forces a real EXDEV commit failure and the old install survives with original bytes.
