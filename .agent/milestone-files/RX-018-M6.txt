AUD-053: destroyed-host DR proof truth. LF-002 previously proved fresh-deployment restore by MANUALLY copying artifacts through the adapters and reconstructing in-memory representations - a destroyed control-plane machine had no demonstrated path to replacement. The journey now proves real destroyed-host DR: (1) export the self-contained BackupBundle (signed manifest + every object's bytes + metadata) from the live source, (2) DESTROY the source deployment root (remove_dir_all; the host is gone - no store, no manifest, no objects left to consult), (3) reconstruct a genuinely fresh target from the bundle ALONE through dr::restore_bundle (hash-verified puts + signed manifest via create_backup + plan validation; the source is never consulted after destruction), (4) decrypt and reattach all five domains in dependency order through production readback surfaces - identity (Principal exact), policy (RelationshipTuple + allowed/denied decision via the real port), memory (MemoryRecord validate), skills (JsonFileSkillRegistryStore load + registration), connectors (capability registry resolve), (5) wrong key fails closed with zero partial restored authority; evidence now carries slug destroyed-host-restore, source_state DESTROYED_BEFORE_RESTORE, dr_mechanism SELF_CONTAINED_BUNDLE, restore_source_consulted FALSE_SOURCE_ROOT_WIPED; real Keycloak/OpenFGA/pgvector/live connectors honestly remain NOT ASSERTED; battery 8 ok / 0 fail, LF-002 journey green, S3 14/14, local 31/31, contract 30/30, clippy -D warnings clean, fmt clean.
