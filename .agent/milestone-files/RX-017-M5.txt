AUD-046: operational SSH provisioning truth. EP-036 provider crates were bindings/validation ONLY - the CloudProviderPort trait had NO implementation anywhere, and M5 evidence left SSH bootstrap NOT ASSERTED; the generic-SSH fallback path (SPEC-016 first-class existing-SSH) is now OPERATIONAL - GenericSshProvider implements CloudProviderPort with REAL transport: submit probes the exact declared target with the real ssh-keyscan binary and returns a receipt at SUBMITTED (never READY), readback re-probes and observes at most REACHABLE (never fabricates READY/VERIFIED), delete is DELETE_ACCEPTED while present and RESOURCE_ABSENT_VERIFIED only after the host is gone; an unreachable target fails closed Unavailable; hostile regressions: non-GenericSsh request denied Policy, unreachable target fails closed, receipt never overclaims; live-fire over a REAL ephemeral alpine+openssh container: submit ok, readback REACHABLE ok, delete accepted ok, absence verified ok (4 new real-transport sentinels wired into the ep036 M3 gate anti-masking list); existing-SSH 8/8, ep036 M1-M5 gates green, clippy -D warnings clean, fmt clean; cloud API provisioning (DigitalOcean/AWS/Contabo/Hetzner) honestly remains NOT ASSERTED (requires live account + provider certification).
