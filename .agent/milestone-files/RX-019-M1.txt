AUD-017: EP-019 IncidentEngine production truth. The IncidentEngine port had NO production implementation anywhere - LF-019 hand-constructed successful SandboxVerdict/SecurityVerdict/approval/canary objects and approval-mismatch checks were the caller's responsibility. StandardIncidentEngine now implements the port and OWNS the canonical lifecycle: observe validates signal shape (non-empty error_class/component, first_seen <= last_seen), derives tenant-scoped dedup keys, deduplicates OPEN incidents by canonical signature (re-observe returns the same incident; a foreign tenant with identical text is a distinct incident), and maps signal kind to risk (security/deployment regression R4); transition enforces the canonical forward edges (Observe->Incident->Correlate->Diagnose->Reproduce->PatchProposed->SandboxValidation->SecurityValidation->Approval->StagedDeployment->PostDeployVerification->Closed) and rejects skipped/backwards/terminal-resurrection moves Conflict; create_diagnosis requires DIAGNOSE state and non-empty hypothesis with one active diagnosis per incident; update_diagnosis_confidence requires an evidence reference and escalates exactly ONE canonical rung per call (Hypothesis->Supported->Reproduced->Validated; jumping to VALIDATED without the intermediate evidence chain fails closed Conflict); propose_patch requires REPRODUCE, non-empty files/diff/digest, tenant+incident match; record_sandbox_validation/record_security_validation require the preceding state, fail closed into VALIDATION_FAILED/SECURITY_FAILED on pass=false, and reject empty passing verdicts; record_approval requires SECURITY_VALIDATION and binds to the EXACT recorded patch digest - a digest mismatch fails closed Policy and the incident never advances (the engine checks the binding; the caller never does); record_post_deploy_verification closes ONLY on verified=true and writes the terminal CLOSED record into backing IncidentMemory via record_final. InMemoryIncidentMemory gained record_final (update terminal state in place; NotFound if never recorded). LF-019 now drives the REAL engine end-to-end: real subprocess crash signal -> engine.observe -> engine transition/diagnosis with real evidence refs -> real patch artifact with real SHA-256 digest -> engine-proposed patch -> real isolated copy apply -> engine-recorded sandbox+security verdicts -> hostile wrong-digest approval rejected Policy by the engine with state unchanged -> correct-digest approval -> real before/after reproduction -> canary -> real post-deploy verification -> engine closes -> engine memory holds CLOSED. Hostile regressions (7 new): full lifecycle closes with real verification + memory terminal record; open dedup by canonical signature; foreign tenant distinct; skipped/backwards/terminal-resurrection transitions rejected; empty-evidence and skip-to-VALIDATED confidence escalation rejected; sandbox failure fails closed VALIDATION_FAILED; approval digest mismatch fails closed Policy + premature approval Conflict; verification=false fails closed and stays open; derived IDs deterministic + retry-stable. nexus-healing 41/41 (unit 27 incl 7 new engine hostile, failure suite, LF-019 live-fire over the real fixture), ep019 M1-M4 gates green, clippy -D warnings clean, fmt clean.
