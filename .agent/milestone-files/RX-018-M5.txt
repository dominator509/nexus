AUD-015: self-contained disaster recovery truth. Backup was a manifest POINTER into a live store - create_backup wrote hashes of objects already present and restore only VERIFIED pre-existing objects on a target that had to be provisioned by hand; LF-002 manually copied artifacts, so a wiped host had no path to reconstruction. The contract now defines BackupBundle (signed manifest + every referenced object's bytes + metadata) with dr::export_backup_bundle (materializes a portable bundle from the live store; fails closed if any manifest hash cannot be materialized - an incomplete bundle is never emitted) and dr::restore_bundle (reconstructs a fresh target from the bundle ALONE - hash-verified puts, signed manifest via create_backup, then plan validation; the source store is never consulted). Provider-neutral: composes the existing ArtifactStore port (list/get/put/create_backup/restore), so local/NAS/S3/SeaweedFS all inherit DR without trait churn; crypto + hashing stay in adapters (M1 dependency-direction gate preserved - contract crate still nexus-domain + serde only). Hostile regressions over REAL filesystem roots: wiped-target reconstruction (export -> WIPE source root -> restore from bundle alone -> artifact serves through the real adapter), export fails closed on deleted object, restore rejects tampered bundle bytes (hash-verified put, zero partial reconstruction), restore rejects unsigned bundle manifest, foreign-tenant bundle denied Policy; contract 30/30, local 31/31 (5 new aud015), M1+M2 gates green, clippy -D warnings clean, fmt clean.
