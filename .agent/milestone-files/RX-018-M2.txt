AUD-050: shared-content delete truth. LocalArtifactStore.delete removed the content object UNCONDITIONALLY - content objects are globally hash-deduplicated, so deleting one artifact could destroy bytes another artifact (same tenant or another tenant) still referenced while the S3 adapter guards with other_refs_exist(); delete now verifies the object, checks other_refs_exist (scanning EVERY tenant's index namespace since objects are shared across the root), removes the index entry ALWAYS, and removes the object ONLY when no other artifact references the hash; RESOURCE_ABSENT_VERIFIED now applies to the index entry and to the object only when unshared (a shared object legitimately remains); hostile regressions over REAL shared roots: same-tenant two-artifacts-same-hash delete keeps object + sibling reads intact + object removed only after last ref gone, cross-tenant delete keeps object + other tenant's get intact; NAS inherits via pass-through; local 23/23 (2 new hostile), NAS 5/5, ep037 M2+M3 gates green, clippy -D warnings clean, fmt clean.
