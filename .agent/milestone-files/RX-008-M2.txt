AUD-084: the canonical control-plane runtime is the APPLICATION COMPOSITION ROOT - main.rs composes RuntimeComposition (REAL InMemoryCapabilityRegistry with registered runtime descriptors, REAL CapabilityDispatcher, REAL McpEngine with tool registry, REAL A2AGatewayImpl with hash-bound MemoryArtifactStore, REAL MemoryOutbox) and the server router exposes the SPEC-003 application surfaces over HTTP: /v1/discover, /v1/mcp/initialize|tools|call, /v1/a2a/tasks (+ run + stream), /v1/artifacts (publish/fetch), /v1/events (append/pending) - every handler drives a REAL engine, never an invented handler or test double; A2AGatewayImpl artifact port gains Send+Sync so the composition satisfies axum State bounds.
