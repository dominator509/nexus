AUD-052: signed backup manifest truth. BackupSet carried NO signature - restore authenticated only hashes/JSON structure, never a signer; the close gate failed closed because the register canonically owns AUD-052 (SPEC-024 DR scope). The contract crate now carries ManifestSignature (Ed25519, hex, validated at construction: 64-char key / 128-char sig) and canonical_manifest_bytes() (deterministic serde with the signature field EXCLUDED - self-exclusion, same rule as the closure attestation digest); verify_manifest_signature_structure() fails closed on missing/malformed/unsupported-algorithm material. Crypto lives in the ADAPTERS (ring 0.17): each create_backup verifies the signature BEFORE the manifest is written (unsigned/tampered never persisted) and each restore verifies BEFORE any hash is trusted. Architecture note: the M1 dependency-direction gate (nexus-artifacts = nexus-domain + serde only) rejected an initial in-contract ring placement; the fix follows the AUD-051 boundary - the contract crate stays crypto-free, adapters own ring. Hostile regressions: contract 30/30 (canonical-bytes determinism + self-exclusion + structure rejection), local 26/26 (3 new: unsigned create rejected + nothing written, garbage-signature create rejected, tampered-on-disk manifest restore rejected), NAS 6/6, S3 14/14, SeaweedFS 21/21, LF-002/LF-020 journeys green (lf002 signs with a REAL Ed25519 keypair), battery 8 ok / 0 fail, clippy -D warnings clean, fmt clean.
