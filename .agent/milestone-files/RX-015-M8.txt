AUD-032: confidence truth. SentinelTriageService confidence derives ONLY from independent planes corroborating the SAME observed source indicator - context events (no shared indicator) join the incident window and contribute observed severity but NEVER inflate confidence. corroborating_planes counts group events only, context events deduped into event_ids without touching the plane set. Hostile: one network plane + unrelated osquery/crowdsec context stays Medium, two planes on the SAME indicator still High, no shared indicator refuses correlation Validation. LF-009 re-run evidence fresh (Suricata plane intact).
