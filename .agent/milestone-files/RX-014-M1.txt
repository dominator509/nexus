AUD-009: the Gmail adapter now matches the REAL Gmail wire format. GmailMessage deserializes the real Gmail JSON (historyId/internalDate as strings, headers from payload.headers as name/value pairs via from_header()/to_headers()/subject() accessors); transport gains send_draft (POST /gmail/v1/users/me/drafts/send with the draft id) - the draft id is a HANDLE, never a recipient; adapter send() calls send_draft instead of fabricating 'To: <draft-id>' raw. Hostile regressions: send resolves stored draft handle, real-wire payload normalizes, missing From fails closed. nexus-gmail 16/16 green.
