AUD-037: osquery observation truth. normalize_row previously FABRICATED the finding's observed_at (hardcoded 2026-08-20T00:00:00Z - never the observation time) and minted collision-prone event ids osquery-{query_id}-{index} (identical across batches and endpoints). The collector now STAMPS the REAL observation time (unix seconds at distributed_write receipt) and a monotonic per-endpoint batch sequence on every observed result (both HTTP route + trait impl); the adapter renders the stamped seconds to RFC3339 UTC via a month/minute-separated formatter (AUD-034 reference) and mints collision-proof ids osquery-{host}-{query_id}-{batch_seq}-{index}; telemetry WITHOUT a stamped observation time fails closed ExternalProvider (audited denial, never fabricated). Hostile regressions: two batches same host same query no id collision, two hosts no id collision, observed_at equals the real stamped seconds exactly (1720000000 = 2024-07-03T09:46:40Z), unstamped time fails closed, formatter never shadows minute. LF-009 evidence carries the REAL observed_at (2026-09-01T14:11:09Z) + collision-proof event_id binding srv-lab-1, fresh run_id m13-aud037.
