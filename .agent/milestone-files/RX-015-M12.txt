AUD-036: osquery TLS plaintext loopback truth. The collector previously bound a PLAINTEXT HTTP listener on 127.0.0.1 - any local process could sniff the enroll secret, node keys, and telemetry or MITM the node. HttpOsqueryEndpoint now generates a self-signed ServerAuth identity (rcgen, SAN localhost+127.0.0.1) at construction, serve() speaks the documented osquery TLS remote API over REAL rustls 0.23 TLS ONLY and FAILS CLOSED (io error) if no TLS identity exists - plaintext is never served; every accepted socket completes a real TLS handshake before any byte reaches routing. Nodes pin the collector certificate via certificate_der() (rustls RootCertStore, server-name verification) - an untrusted client is denied at the handshake. Hostile regressions over REAL sockets: plaintext HTTP POST receives NO HTTP response / NO node_key / NO node_invalid, untrusted TLS client handshake denied, pinned-TLS full enroll/read/write lifecycle green. LF-009 node side plays the enrolled osqueryd over REAL TLS with the pinned collector cert - evidence tls=REAL_TLS_PINNED and certification TRANSPORT_CERTIFIED over REAL TLS sockets.
