AUD-082: the release manifest is now built and verified from the REAL committed product artifacts - wake model code (decision.py, manifest.py), provider config (providers.json), router policy (policy.json), and the SeaweedFS container definition - never from fixture strings. The manifest CLI resolves components through a real-artifact map; verify-manifest fails closed for any component not in the map or whose real bytes differ. Hostile proofs: fixture-only manifest rejected, deleted real artifact -> NOT_FOUND, tampered digest -> VERIFICATION_FAILED, ghost component denied; fresh clone rebuilds the identical manifest from committed bytes.
