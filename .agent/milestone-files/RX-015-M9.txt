AUD-033: verification binding truth. SentinelVerificationService.verify_response binds the plan to the EXACT applied proposal - plan.quarantine_proposal_ref must equal the registered applied proposal id AND tenants must match, else fails closed Verification BEFORE any firewall readback; proposal B evidence can never produce VERIFIED for plan A. LF-009 binds the plan via with_quarantine(proposed.proposal_id) and adds a hostile live-fire check (plan bound to proposal-other refused against the real engine). Hostile unit regressions: different-proposal denial and no-ref denial gated before readback (NeverVerifyingFirewall answers never-verified so the bound path yields Failed). Evidence plan_binds_proposal true with fresh run_id m9-aud033.
