[package]
name = "nexus-capabilities"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Nexus capability and connector domain: capability registry, capability descriptors, connector manifests, query/command/workflow/health/change-feed ports, invocation context, and connector-tier contracts (SPEC-003, SPEC-022, EP-010)"

[dependencies]
serde.workspace = true
serde_json.workspace = true
nexus-domain = { path = "../nexus-domain", version = "0.1.0" }
nexus-identity = { path = "../nexus-identity", version = "0.1.0" }

[dev-dependencies]
