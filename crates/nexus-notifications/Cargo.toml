[package]
name = "nexus-notifications"
version = "0.1.0"
edition = "2021"
description = "EP-032 provider-neutral notification contracts: NotificationRouter, NotificationEnvelope, ChannelProvider, DeliveryPolicy, PrivacyRouting, EscalationPolicy, DeliveryReceipt (SPEC-014)"
license = "MIT"

[dependencies]
nexus-domain = { path = "../../crates/nexus-domain", version = "0.1.0" }
serde.workspace = true
serde_json.workspace = true
