[package]
name = "nexus-model-gateway"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "EP-013 contract crate: provider-neutral model provider, gateway, registry, health, budget, request/response, and tool-call envelope contracts (SPEC-009, EP-013). Bifrost is preferred but hidden behind ModelGateway; direct providers remain available; provider credentials never leave the gateway."

[dependencies]
serde.workspace = true
serde_json.workspace = true
nexus-domain = { path = "../nexus-domain", version = "0.1.0" }
nexus-identity = { path = "../nexus-identity", version = "0.1.0" }
