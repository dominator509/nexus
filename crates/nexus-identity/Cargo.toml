[package]
name = "nexus-identity"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Nexus identity domain: principals, people, households, businesses, devices, sessions, presence evidence, and tenant boundaries (SPEC-001/005, EP-003)"

[dependencies]
serde.workspace = true
serde_json.workspace = true
nexus-domain = { path = "../nexus-domain", version = "0.1.0" }

[dev-dependencies]
postgres = { version = "0.19.14", features = ["with-serde_json-1"] }
