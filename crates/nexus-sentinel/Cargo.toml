[package]
name = "nexus-sentinel"
version = "0.1.0"
edition = "2021"
description = "EP-030 provider-neutral sentinel core contracts: FirewallProvider, DnsSecurityProvider, NetworkInventory, DeviceFingerprint, BehaviorBaseline, NetworkFinding, QuarantineProposal (SPEC-013)"
license = "MIT"

[dependencies]
nexus-domain = { path = "../../crates/nexus-domain", version = "0.1.0" }
serde.workspace = true
serde_json.workspace = true
