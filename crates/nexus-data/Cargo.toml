[package]
name = "nexus-data"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Nexus data and memory contracts: UnitOfWork, repository ports, memory records, retention, and world graph abstraction (SPEC-002, EP-004)"

[dependencies]
serde.workspace = true
serde_json.workspace = true
nexus-domain = { path = "../nexus-domain", version = "0.1.0" }
