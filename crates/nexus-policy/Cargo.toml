[package]
name = "nexus-policy"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Nexus authorization policy domain: relationship authorizer, context policy engine, risk classifier, capability grants, approval assertions, action gateway, receipts, and verification plans (SPEC-005, SPEC-006, EP-008)"

[dependencies]
serde.workspace = true
serde_json.workspace = true
nexus-domain = { path = "../nexus-domain", version = "0.1.0" }
nexus-identity = { path = "../nexus-identity", version = "0.1.0" }
nexus-auth = { path = "../nexus-auth", version = "0.1.0" }

[dev-dependencies]
