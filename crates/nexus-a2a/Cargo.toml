[package]
name = "nexus-a2a"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "EP-012 A2A gateway: protocol 1.0.1 agent tasks, streaming status, artifacts, cancellation, and push notifications mapped to canonical tasks (SPEC-003, EP-012)"

[dependencies]
serde.workspace = true
serde_json.workspace = true
nexus-domain = { path = "../nexus-domain", version = "0.1.0" }
nexus-auth = { path = "../nexus-auth", version = "0.1.0" }
nexus-fabric = { path = "../nexus-fabric", version = "0.1.0" }
