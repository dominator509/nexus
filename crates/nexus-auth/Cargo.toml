[package]
name = "nexus-auth"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Nexus authentication and passkey domain: OIDC client, token validation, passkey enrollment, device enrollment, sessions, step-up challenges, and recovery kits (SPEC-005, EP-007)"

[dependencies]
serde.workspace = true
serde_json.workspace = true
nexus-domain = { path = "../nexus-domain", version = "0.1.0" }
nexus-identity = { path = "../nexus-identity", version = "0.1.0" }

[dev-dependencies]
