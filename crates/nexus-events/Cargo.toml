[package]
name = "nexus-events"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Nexus event nervous system contracts: envelopes, outbox, inbox, consumers, and stream provisioning (SPEC-023, EP-005 M1)"

[dependencies]
serde.workspace = true
serde_json.workspace = true
nexus-domain = { path = "../nexus-domain", version = "0.1.0" }
nexus-data = { path = "../nexus-data", version = "0.1.0" }
