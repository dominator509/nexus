//! EP-018 skill evaluation contracts (SPEC-010 behavior 8; ADR-025).
//!
//! The evaluation port and result type are owned here; the
//! deterministic implementation lives in the composer module.

pub use crate::proposal::SkillEvaluation;
pub use crate::proposal::SkillEvaluator;
pub use crate::proposal::SkillEvaluatorError;
