{
  "name": "@nexus/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Nexus web dashboard contracts (EP-033 M1) + the ACTUAL React PWA entry (AUD-038): provider-neutral dashboard shell, chat, objectives, approvals, fleet, security, provider settings, and audit explorer contracts over the generated canonical bindings. The PWA entry (index.html + src/main.tsx + Vite build) mounts the REAL @nexus/ui React components over the contract layer; framework-neutral contracts never import React/DOM.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test:unit": "vitest run src/__tests__",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json && vite build",
    "pwa:build": "vite build"
  },
  "dependencies": {
    "@nexus/contracts": "workspace:*",
    "@nexus/ui": "workspace:*",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "vitest": "^3.0.0",
    "vite": "^7.3.6",
    "@vitejs/plugin-react": "^4.3.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0"
  }
}
