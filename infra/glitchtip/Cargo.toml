[package]
name = "nexus-glitchtip"
version = "0.1.0"
edition = "2021"
description = "EP-038 M3 GlitchTip/Sentry-compatible incident sink: DSN handling, Sentry envelope serialization, std::net transport, and M1 IncidentSink integration with redaction-before-egress (SPEC-007; node contract)"
license = "MIT"

[dependencies]
nexus-domain = { path = "../../crates/nexus-domain", version = "0.1.0" }
nexus-observability = { path = "../../crates/nexus-observability", version = "0.1.0" }
serde_json = "1.0"

[dev-dependencies]
nexus-otel = { path = "../otel", version = "0.1.0" }
