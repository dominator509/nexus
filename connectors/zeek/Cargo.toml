[package]
name = "nexus-zeek-connector"
version = "0.1.0"
edition = "2021"
description = "EP-031 Zeek connector: advanced-profile network detection adapter behind the nexus-sentinel-advanced NetworkDetectionProvider port (SPEC-013)"
license = "MIT"

[dependencies]
nexus-domain = { path = "../../crates/nexus-domain", version = "0.1.0" }
nexus-sentinel = { path = "../../crates/nexus-sentinel", version = "0.1.0" }
nexus-sentinel-advanced = { path = "../../infra/sentinel/advanced", version = "0.1.0" }
serde.workspace = true
serde_json.workspace = true
