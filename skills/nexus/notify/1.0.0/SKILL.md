# Notify

Notification delivery skill. Declares READ and WRITE; installing this
skill requires a TRUSTED or SYSTEM caller because WRITE exceeds the
SANDBOXED ceiling. The declaration alone never grants WRITE.
